<%= form_for(current_user.active_like_posts.find_by(post_id: micropost.id),
             html: { method: :delete },remote: true) do |f| %>
  <%= f.submit "k thich", class: "btn btn-link like" %>
<% end %>